<%= form_with(model: @city, local: true) do |form| %>
  <% if @city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
      <% city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="container" style="margin-top:50px">
 <div class="row">
  <div class="col-md-12" >
   <div class="panel panel-primary">
    <div class="panel-heading" style="font-size:20px;">New City </div>
     <div class="panel-body">
	    <label>Name</label><%= form.text_field :name, id: :city_name, class:"form-control" %>
      <label>Description</label><%= form.text_area :description, id: :city_description, class:'form-control' %>
      <button class="btn btn-lg btn-primary btn-block" style="margin-top:20px;" type="submit">ADD</button>
      <div class="col-sm-6" style="padding:20px 5px 10px 0">
        <%= link_to 'Show', @city, class:"btn btn-lg btn-success btn-block" %>
      </div>
      <div class="col-sm-6" style="padding:20px 0 10px 5px">
        <%= link_to 'Back', cities_path , class:"btn btn-lg btn-warning btn-block" %>
      </div>
    </div>
   </div>
  </div>
 </div>
</div>
<% end %>
