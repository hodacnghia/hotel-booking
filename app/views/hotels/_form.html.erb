<%= form_with(model: hotel, local: true) do |form| %>
  <% if hotel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hotel.errors.count, "error") %> prohibited this hotel from being saved:</h2>

      <ul>
      <% hotel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<!-- <div class="field">
    <%= form.label :city_id %>
    <%= collection_select(:hotel, :city_id, City.all, :id, :name, {prompt: true}) %>

  </div>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :hotel_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :hotel_description %>
  </div>

  <div class="field">
    <%= form.label :picture %>
    <%= form.file_field :picture,multiple: true, id: :home_stay_picture %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div> -->

<div class="container" style="margin-top:50px">
 <div class="row">
  <div class="col-md-12" >
   <div class="panel panel-primary">
    <div class="panel-heading" style="font-size:20px;">New Hotel</div>
     <div class="panel-body">
      <label>City</label><%= collection_select(:hotel, :city_id, City.all, :id, :name, {prompt: true} , class:"form-control") %>
	    <label>Name</label><%= form.text_field :name, id: :hotel_name, class:"form-control" %>
      <label>Phone</label><%= form.text_field :phone, id: :hotel_phone, class:"form-control" %>
      <label>Description</label>
      <%= form.text_area :description, id: :hotel_description, class:'form-control ' %>
      <label>Picture</label><%= form.file_field :picture,multiple: true, id: :home_stay_picture, class:"form-control" %>
      <button class="btn btn-lg btn-primary col-sm-6" style="margin-top:20px" type="submit">ADD</button>
      <div class="col-sm-6" style="padding:20px 0 10px 5px">
        <%= link_to 'Back', root_path, class:"btn btn-lg btn-warning btn-block" %>
      </div>
    </div>
   </div>
  </div>
 </div>
</div>
<% end %>


<script>

        CKEDITOR.replace('hotel_description')

var loadFile = function(event) {
  var output = document.getElementById('image');
  output.src = URL.createObjectURL(event.target.files[0]);
  $('avatar img-responsive img-rounded')
  .attr('image', e.target.result)
  .width(150)
  .height(200);
  };
</script>
